# Agent instructions

Read `CLAUDE.md` first. It is the canonical guide for this repository.

Site content under `content/` is German, everything else is English. Use the
Makefile targets for all tasks and run `make fmt` before committing. Do not
commit `dist/`, `public/`, or `static/processed_images/`.
